import random
matrix = [[random.randint(-10, 10) for _ in range(4)] for _ in range(4)]
for x in matrix:
    print(x)
y = 1
for x in matrix:
    for element in x:
        if element > 0:
            y *= element
print("Произведение положительных элементов:", y)

