import numpy as np

M = 4
N = 5
matrix = np.random.randint(0, 10, (M, N))

print("Матрица A:")
print(matrix)

n = np.count_nonzero(row) for row in matrix

print("Количество ненулевых элементов в каждой строке:")
print(n)